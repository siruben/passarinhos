const { Constraint, World, Body, Vector } = Matter; export function createSlingshot({ engine, render, anchor, body }){ const world=engine.world; const maxPull=120; const power=0.045; let dragging=false; let released=false; const constraint=Constraint.create({ pointA:{ x:anchor.x, y:anchor.y }, bodyB:body, stiffness:0.02, damping:0.02, render:{ strokeStyle:'#5fd3ff', lineWidth:4 } }); World.add(world,constraint); Body.setInertia(body, Infinity); const mouse=render.mouse; function isMouseOverBody(){ const pos=mouse.position; const dx=pos.x-body.position.x; const dy=pos.y-body.position.y; const r=body.circleRadius||20; return (dx*dx+dy*dy) <= (r*r); } function clampPull(p){ const dir=Vector.sub(p,anchor); const mag=Vector.magnitude(dir); if(mag<=maxPull) return p; const nd=Vector.mult(Vector.normalise(dir), maxPull); return Vector.add(anchor, nd); } const onMouseDown=()=>{ if(released) return; if(!isMouseOverBody()) return; dragging=true; }; const onMouseMove=()=>{ if(!dragging || released) return; const p=clampPull(mouse.position); Body.setPosition(body,p); Body.setVelocity(body,{ x:0, y:0 }); }; const onMouseUp=()=>{ if(!dragging || released) return; dragging=false; released=true; const pull=Vector.sub(anchor, body.position); const impulse=Vector.mult(pull, power); Body.setInertia(body, body.mass * (body.circleRadius ** 2)); Body.setVelocity(body, impulse); constraint.bodyB=null; }; const el=render.canvas; el.addEventListener('mousedown', onMouseDown); el.addEventListener('mousemove', onMouseMove); window.addEventListener('mouseup', onMouseUp); body.plugin=body.plugin||{}; body.plugin.slingReleased=()=>released; return { update(){}, detach(){ try{ World.remove(world,constraint);}catch{} el.removeEventListener('mousedown', onMouseDown); el.removeEventListener('mousemove', onMouseMove); window.removeEventListener('mouseup', onMouseUp); } }; }